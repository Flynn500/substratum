[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "substratumx"
version = "0.1.3"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
include = ["substratum.pyi"]
module-name = "substratum"
